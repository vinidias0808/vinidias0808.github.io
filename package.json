{
  "name": "vinicius-dias-site",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page pessoal - criacao de sites para empresas",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./assets/styles.css --minify",
    "dev": "tailwindcss -i ./src/input.css -o ./assets/styles.css --watch",
    "vercel-build": "npm run build && node scripts/stage.mjs"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
